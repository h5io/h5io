language: python

# Use container-based infrastructure
sudo: false

env:
    - PYTHON=2.7
    - PYTHON=3.5
    - PYTHON=3.6

# Setup anaconda
before_install:
  - wget -q http://repo.continuum.io/miniconda/Miniconda-latest-Linux-x86_64.sh -O miniconda.sh
  - chmod +x miniconda.sh
  - ./miniconda.sh -b -p /home/travis/miniconda
  - export PATH=/home/travis/miniconda/bin:$PATH
  - conda update --yes --quiet conda

install:
    - INSTALLS="nose numpy h5py pep8 pyflakes flake8 coverage scipy pandas pytables conda-build git anaconda-client"
    - conda create -n testenv --yes pip python=$PYTHON $INSTALLS
    - source activate testenv
    - conda config --set anaconda_upload yes
    - pip install -q codecov
    - python setup.py build
    - python setup.py install

script:
    - nosetests --verbosity=2 --with-coverage
    - make flake

after_success:
    - codecov
    - conda build --python ${PYTHON} --user jan-janssen --token ${CONDA_UPLOAD_TOKEN} conda/h5io
